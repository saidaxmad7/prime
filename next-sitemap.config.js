/** @type {import('next-sitemap').IConfig} */
const config = {
  siteUrl: 'https://prime-game-club.uz',
  generateRobotsTxt: true,
  changefreq: 'daily',
  priority: 0.7,
};

module.exports = config;
